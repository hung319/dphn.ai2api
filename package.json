{
  "name": "dphn-openai-proxy",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "docker:build": "docker build -t dphn-proxy .",
    "docker:run": "docker run -p 3000:3000 -d dphn-proxy"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
